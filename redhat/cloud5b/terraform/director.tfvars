# director host IP
director_host = "131.160.194.206"

# bridge interface for the external/ECN network on the director host e.g. brpub
ext_bridge = "brmgmt5b"

# bridge interface for the provisioning network on the director host e.g. brprov
prov_bridge = "brprov5b"

# bridge interface for the external/ECN network on the director host e.g. brint
int_bridge = "brint5b"

